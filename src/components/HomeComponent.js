import React, { PureComponent } from 'react';

const Home = (props) => {
    return (
        <div className="container">
            <h4 className="">Home</h4>
        </div>
    );
}

export default Home;
